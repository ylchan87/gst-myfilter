bin_PROGRAMS = gst-myfilter

# sources used to compile this plug-in
gst_myfilter_SOURCES = gstmyfilter.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_myfilter_CFLAGS = $(GST_CFLAGS)
gst_myfilter_LDADD = $(GST_LIBS)

